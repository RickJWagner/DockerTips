# Start the container                  'docker run -it fsw_ru5 /bin/bash'   
# Start the container                  'docker run -it --net="none" -p 8080:8080 fsw_ru5 /bin/bash'                          // No network!
